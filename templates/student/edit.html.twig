{% extends 'base.html.twig' %}

{% block title %}Edit Student{% endblock %}

{% block body %}
    <div class="container mt-5">
        <div class="card">
            <div class="card-header bg-primary text-white">
                <h1 class="display-4 mb-0">Edit Student</h1>
            </div>
            <div class="card-body" >
                {{ include('student/_form.html.twig', {'button_label': 'Update'})|raw }}
            </div>
            <div class="card-footer">
                <a class="btn btn-secondary" href="{{ path('app_student_index') }}">Back to List</a>
            </div>
        </div>
    </div>

    {{ include('student/_delete_form.html.twig')|raw }}
</div>
{% endblock %}

